# Raw downloads are never edited after fetch and are too large for git.
# Reproduce them with: python src/ingest.py 2026-01
/data/raw

# Generated by validate.py; reproduce with: python src/validate.py 2026-01
/data/processed
/data/rejected

# NOTE: logs/pipeline_run.log IS tracked on purpose - the assignment's required
# repo structure lists it as run evidence. It is appended to, never truncated.

/src/venv
.ipynb_checkpoints
__pycache__/
*.pyc
.DS_Store

# Local copy of the assignment brief (kept out of the submission repo)
assignment.md
